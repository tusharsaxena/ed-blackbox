# scripts/

Reusable scripts for building and maintaining the Elite:Dangerous Black Box.

**Convention:** every script used for a project task lives here, named specifically for
what it does. Each script has its own documentation file with the **same name and a
`.md` extension** (e.g. `generate-guides-index.sh` → `generate-guides-index.md`).
Don't leave task scripts in `/tmp` or run them inline-only — save them here so they can
be re-run later.

| Script | Docs | What it does |
|---|---|---|
| `generate-guides-index.sh` | [generate-guides-index.md](generate-guides-index.md) | Regenerates `guides/index.html`, the "Black Box" landing page linking to every guide. |
| `generate-anchor-files.sh` | [generate-anchor-files.md](generate-anchor-files.md) | Regenerates the per-page `<basename>-anchors.md` files cataloging each guide's `<section id>` navigation anchors. |
| `header-crumb-from-breadcrumbs.py` | [header-crumb-from-breadcrumbs.md](header-crumb-from-breadcrumbs.md) | One-time migration: replaces the in-header **"On this page"** eyebrow with a breadcrumb-derived **`.hdr-crumb`** block (current page as title + parent trail beneath, links dropped). Derives content from each page's own `.breadcrumbs` nav; idempotent + byte-preserving; skips the generated `guides/index.html` (its crumb comes from `generate-guides-index.sh`). `--check` previews. **Obsolete** since `deprecate-breadcrumbs.py` removed the `.breadcrumbs` nav. |
| `deprecate-breadcrumbs.py` | [deprecate-breadcrumbs.md](deprecate-breadcrumbs.md) | One-shot migration: retires the standalone `<nav class="breadcrumbs">` strip site-wide and makes the in-header **`.hdr-crumb`** the only breadcrumb — its parent trail becomes navigable links (relative, same-tab, **no Home**) reusing the nav's own hrefs/labels; the nav (+ its comment) is removed. Top-level pages → `hdr-crumb solo`. Idempotent + byte-preserving; `--check` previews. Skips generated `guides/index.html`; leaves the stale `generate-ship-role-matrix.py`. |
| `generate-ship-role-matrix.py` | [generate-ship-role-matrix.md](generate-ship-role-matrix.md) | Builds `guides/ships/ship-role-matrix.html` — the interactive Ship × Role Matrix (rows = hulls, columns = roles) projected from `data/ship-ratings/`. Dossier-backed cells only; pad class cross-checked against `data/ships/`. Header sort/filter glyphs + sticky header. `--page` writes the page; `--mockups` a throwaway preview. |
| `extract-title-blocks.py` | [extract-title-blocks.md](extract-title-blocks.md) | Extracts every guide's title block (the `<title>`, breadcrumb crumb, and `<header class="masthead">` decomposed into kicker/h1/subtitle/meta) into `scripts/out/title-blocks.json` — the "current" state for the title-block standardization task. |
| `title-block-standardize.py` | [title-block-standardize.md](title-block-standardize.md) | Builds `scripts/out/title-blocks.xlsx`, a side-by-side CURRENT vs PROPOSED view of every page's masthead elements (+ a Rules sheet) for human review; merges per-page workflow proposals from `proposed-overrides.json`. |
| `apply-title-blocks.py` | [apply-title-blocks.md](apply-title-blocks.md) | Applies the reviewed title-block targets (`scripts/out/edited-rows.json`) to the pages. Element-targeted + idempotent: rewrites only the `<title>`/kicker/h1/subtitle/meta that changed, normalizes dates to ISO, escapes bare ampersands, bolds the meta keyword. Run with `--files` for a batch, `--dry-run` to preview. |
| `verify-title-blocks.py` | [verify-title-blocks.md](verify-title-blocks.md) | Verifies pages match the reviewed title-block targets after apply (title, kicker, 2-span meta, h1 when changed, subtitle drop, no bare `&`/Excel dates). Reuses the apply build logic; exits nonzero on any FAIL. |
| `apply-role-tweaks.py` | [apply-role-tweaks.md](apply-role-tweaks.md) | Path-driven, idempotent role-presentation tweaks: activity h1 role → "Activity Guide" + breadcrumb/nav re-homed under Systems; dossier h1 `.role` given a `ac-role-<role>` colour class; by-role kicker → "Best Ships by Role". (Index move lives in `generate-guides-index.sh`.) |
| `add-breadcrumb-subsection.py` | [add-breadcrumb-subsection.md](add-breadcrumb-subsection.md) | Inserts the index sub-section crumb into every guide breadcrumb (`Home › Section › Sub-section › Page`), linking the sub-head anchor. Builds the page→sub-section map from the index generator at runtime; idempotent + self-healing. |
| `shot.mjs` | [shot.md](shot.md) | Generates a full-page PNG screenshot of a local HTML file via Playwright/Chromium; used for before/after visual comparison during design-system migration. |
| `fingerprint.mjs` | [fingerprint.md](fingerprint.md) | Extracts a content fingerprint (masthead text + each `<section id>`/text + all ids) of a guide, chrome excluded; the before/after content-invariance gate for migration. |
| `fp-diff.mjs` | [fingerprint.md](fingerprint.md) | Compares two fingerprints; exits non-zero if any section text or anchor id changed. Pairs with `fingerprint.mjs`. |
| `baseline-capture.sh` | [baseline-capture.md](baseline-capture.md) | Captures a pre-migration screenshot + fingerprint for all 108 guides into `_migration/baseline/` (gitignored); the comparison baseline for the migration. |
| `fix-step-tuples.mjs` | [fix-step-tuples.md](fix-step-tuples.md) | One-shot content fix: unpacks malformed `(action, rationale, cost)` tuple text in dossier step lists into readable markup (quote-aware). |
| `trim-svg.mjs` | [trim-svg.md](trim-svg.md) | Tightens inline diagram `<svg>` viewBoxes to their content + ~5px margin (via Playwright `getBBox`), removing phantom whitespace. |
| `restructure-app-cards.mjs` | [restructure-app-cards.md](restructure-app-cards.md) | One-shot simplifier for the third-party-apps tool cards: amber accent, stable `id`s, tag row, ↗ open-glyph, drops the decorative eyebrow. |
| `classify-card-groups.mjs` | [classify-card-groups.md](classify-card-groups.md) | Sets each `.cards` container's column-count class (`cards four` / `three` / `two` / `one`) from its card count; respects hand-set counts and migrates legacy `wide`/`extra-wide`. Class-only, gate-neutral, re-runnable. |
| `sort-compare-tables.py` | [sort-compare-tables.md](sort-compare-tables.md) | Reorders the rows of every "How It Compares" `table.cmp` (ship dossiers + by-role) by `.rscore` rating, descending, with dash (`&mdash;`) ratings last. Reorder-only, stable, idempotent. |
| `convert-dossier-rating-cards.sh` | [convert-dossier-rating-cards.md](convert-dossier-rating-cards.md) | One-off migration converting each dossier's briefing rating from the `.dial` wheel to the number + `.bar.mini` ladder form (`guides/ships/dossiers/*.html`). |
| `modernize-by-role-cards.sh` | [modernize-by-role-cards.md](modernize-by-role-cards.md) | One-off by-role markup cleanup (`guides/ships/by-role/*.html`): `<p class="subhd">` → `<h3 class="subhead">`, and strips the leading `· ` in each `.pick` `<small>` (cost now rendered on its own row). Markup-only, re-runnable. |
| `migrate-dossier-panels.sh` | [migrate-dossier-panels.md](migrate-dossier-panels.md) | One-off `.subhd` retirement for dossiers (`guides/ships/dossiers/*.html`): `<p class="subhd">` → `<h3 class="subhead">`, and the `.cols-2` bullet-panel pair → `.cards two` of `.card`+`.ico`. Markup-only, re-runnable. |
| `migrate-docking-panels.sh` | [migrate-docking-panels.md](migrate-docking-panels.md) | One-off `.subhd` retirement for the docking manual: panels → `.card`s (blue/green paths → `.ac-fed`/`.ac-good`), in-panel `h3.subhead` titles → `h4`. Markup-only, re-runnable. |
| `add-ship-render.py` | [add-ship-render.md](add-ship-render.md) | Inserts a framed ship render (`images/ships/`) into each dossier's briefing (`.verdict.has-render` + `.ship-figure`), with a `Name &middot; Manufacturer` caption. Slug→render mapping, idempotent; skips hulls with no render. |
| `inject-rating-methodology-links.sh` | [inject-rating-methodology-links.md](inject-rating-methodology-links.md) | Inserts a cross-link to `ships/rating-methodology.html` into every page that shows a 1–100 rating: a `.callout` after each dossier's verdict box (77) and a link line after each by-role "How These Ships Are Scored" section (7). Idempotent; adds no `<section id>`. |
| `build-module-spec-tables.mjs` | [build-module-spec-tables.md](build-module-spec-tables.md) | Rebuilds all 30 module cards' spec tables on `guides/engineering/modules.html` from the local EDCD/coriolis dataset (`data/modules/{standard,internal,hardpoints}/*.json`) via a baked-in card→file map + per-card column selection (Class, Rating, Mass/Integrity/Power, ≤5 relevant stats, Value last). Strips prior spec blocks + stale Availability/Core-stats panels, moves callouts to the end, appends pre-sorted "Concept D" fade-reveal table(s), injects the page-scoped CSS (incl. 2-line `<th>` clamp) + pill JS once. Idempotent (byte-identical re-runs); prints cards/columns built. |
| `rebuild-footers.py` | [rebuild-footers.md](rebuild-footers.md) | Rewrites every guide's `<footer>` to the design-system two-tier footer (brand · byline→INARA · part · issue tracker, over a copyright + Frontier fan-content disclaimer line). Preserves each page's part label; idempotent. Keep the templates + `generate-guides-index.sh` footer in sync. |
| `linkify-section-refs.py` | [linkify-section-refs.md](linkify-section-refs.md) | Converts inline `§NN` prose references into readable, clickable `Section N` links targeting the matching on-page `<section>`. Protects HTML comments + existing `<a>` spans; idempotent. Run `audit-section-numbers.py` first. |
| `audit-section-numbers.py` | [audit-section-numbers.md](audit-section-numbers.md) | Read-only gate: verifies each guide's `sec-num` values are continuous `1..N` (standard 2-digit form) and that numeric `qn-side` quick-nav numbers match. Exits non-zero on any gap/dup/mismatch. |
| `depersonalize-home-base.sh` | [depersonalize-home-base.md](depersonalize-home-base.md) | One-shot editorial pass removing the author's personal "your Diaguandri / Ray Gateway base/home" framing (52 dossiers + 3 systems pages) and reworking each into role-neutral phrasing. Leaves factual Diaguandri/Ray Gateway mentions (Landmines market, Jameson nearest trader) and the kept `CMDR Ka0s` byline alone. Idempotent. |
| `verify-links.py` | [verify-links.md](verify-links.md) | Full hyperlink audit: resolves every internal `<a href>` (same-page, relative, `/ed-blackbox/guides/` absolute) to a real file + existing `id`, checks quick-nav (`.qn-item`) anchors resolve and their labels match the target heading, and inventories unique external URLs to `external-urls.txt` for a separate liveness pass. Read-only; exits nonzero on any broken internal link/anchor. |
| `normalize-link-targets.py` | [normalize-link-targets.md](normalize-link-targets.md) | Enforces the link-open policy site-wide: internal links → same tab, external → `target="_blank" rel="noopener noreferrer"`. Byte-preserving, idempotent. Run on any new/edited page. |
| `build-link-dictionary.py` | [build-link-dictionary.md](build-link-dictionary.md) | Extracts the canonical hyperlink targets (engineers, blueprints, modules, materials, powers, superpowers, ship dossiers) into `data/links/link-dictionary.base.json`; augmented by hand-curated `data/links/link-aliases.json`. |
| `apply-hyperlinks.py` | [apply-hyperlinks.md](apply-hyperlinks.md) | Cross-links references to the source pages — wraps each occurrence in an internal `<a>` to the right anchor (fuzzy + context-aware, ≥ 0.75 applied), logging every candidate to `data/links/link-candidates.csv`. Byte-preserving; skips nav/headings/stat-tiles/credits/existing links. |
| `build-link-report.py` | [build-link-report.md](build-link-report.md) | Rolls `data/links/link-candidates.csv` up into `data/links/hyperlink-opportunities.xlsx` (Summary · Applied ≥0.75 · Below bar <0.75 · All candidates) for post-review. |
| `link-by-role-pages.py` | [link-by-role-pages.md](link-by-role-pages.md) | Curates the role-correct cross-links on `guides/ships/by-role/**` (excluded from the generic applier): ship cells in the ladder/pad tables + the headline pick → the `<ship>-<role>` dossier for that page; the Cost & Engineering table → module/blueprint/engineer anchors, stripping bold/accent emphasis. Deterministic, idempotent, byte-preserving. |
| `link-activity-pages.py` | [link-activity-pages.md](link-activity-pages.md) | Runs the `apply-hyperlinks.py` engine over the six `guides/activities/**` role playbooks (excluded from the generic applier) with the page role taken from the filename, so bare ship names resolve to the `<ship>-<role>` dossier for that activity. Blocks the bare activity nouns "mining"/"xeno" (they mean the activity, not the weapon module). |
| `standardize-anchors.py` | [standardize-anchors.md](standardize-anchors.md) | Puts every navigation anchor onto one `<family>-<slug>` scheme (`section-`, `engineer-`, `blueprint-`/`blueprint-group-`, `module-`/`module-group-`, `powerplay-`, `superpower-`, `step-`, `app-`) and rewrites all internal hyperlinks to match, with zero broken links. Page+element-aware classification; builds a reusable old→new map (`--build-map`), applies ids (`--phase1`) and href fragments (`--phase2`) in separate phases, and self-verifies (`--verify`). Excludes functional ids (`qn-*`, `toc`, checklist diagram `n-*`/`c-*`/`emap-inner`/`wires`). Idempotent. |
| `strip-page-accents.py` | [strip-page-accents.md](strip-page-accents.md) | One-shot cleanup: removes every per-page accent override `<style>` block (59 pages), dropping all guides to the design-system default amber accent. Matches only the `:root{--accent:…}` block — bespoke layout `<style>` blocks (index grids, engineering tables) are left alone. The accent feature itself stays available via `ed-blackbox.css`. Idempotent (no-op once clean). |
| `align-table-headers.py` | [align-table-headers.md](align-table-headers.md) | Makes each `table.data`/`l3`/`cmp` header cell follow its column's data alignment — adds `num`/`center` to a `<th>` when the column's `<td>`s are uniformly right/centre-aligned. Skips tables that re-align in page CSS (`spec.cD`, `matrix`, `mx`). Header-tag-only surgery (entities/format preserved); idempotent. |
| `fix-generic-sources.py` | [fix-generic-sources.md](fix-generic-sources.md) | Repoints bottom-of-page **Sources** `.cr-link`s that cite a website/repo **home** at the specific resource the figures came from (e.g. coriolis-data root → `…/ships/<ship>.json`; EDDN status URL → `github.com/EDCD/EDDN`), and drops rows where no specific target exists. Data-driven from `fix-generic-sources.ops.json`; idempotent, `--check` re-verifies every new URL resolves (HTTP 200). |
| `drop-dist-column.py` | [drop-dist-column.md](drop-dist-column.md) | Removes the **`Dist.`** column (max power-distributor class, `PDn`) from the full-ladder ranking table on every `guides/ships/by-role/*.html` page and rebalances the five remaining columns (the freed 7% goes to the verdict column: `17/10/16/45/12`). Keys on the table's unique signatures (the `7%` col width, `<th class="num">Dist.</th>`, `<td class="num">PD[0-9]+</td>`) so the per-class/cost tables — which also use `class="num"` — are untouched. Idempotent. |
| `audit-ratings-consistency.py` | [audit-ratings-consistency.md](audit-ratings-consistency.md) | Read-only cross-page audit of the 1–100 ship **suitability ratings**. For each role it gathers every `(role, ship)` rating assertion from the `by-role/<role>.html` ladder + per-class tables and from every `<ship>-<role>.html` dossier (headline + all peer-comparison rows), groups by normalised ship name, and flags any group whose values disagree — with full provenance (value · source · file), split into **tier 1** (ship has a dossier → headline is truth) and **tier 2** (no dossier → ambiguous). Exit 0 = consistent, 1 = mismatch. `--json` for machine-readable output. Diagnostic only; never edits. |
| `compute-ship-ratings.py` | [compute-ship-ratings.md](compute-ship-ratings.md) | Builds `data/ship-ratings/<role>.json` — the **source of truth** for ship ratings. Canonical value = the dossier **headline** where a dossier exists; a dossier-less hull keeps its value only if every page agrees, else it is excluded (no authority to adjudicate). Read-only on the HTML; writes the data files. |
| `build-ship-scorecards.py` | [build-ship-scorecards.md](build-ship-scorecards.md) | Injects the **"Why This Rating" scorecard** section into each ship dossier from `data/ship-ratings/` (`scorecard_weights` per role + `ratings[].scorecard` per ship — earned points sum to the headline rating). Renders the approved Variant-A table (`table.data.scorecard`) **before** `#section-how-it-compares`, automatically **renumbering** later `.sec-num` and inserting the quick-nav `qn-item` (+ bumping later `qn-side`). Section ids are slug-based so no cross-links break. Idempotent (insert → replace); `--check` previews. Re-run `generate-anchor-files.sh` after a first insert. |
| `apply-scorecard-authoring.py` | [apply-scorecard-authoring.md](apply-scorecard-authoring.md) | Merges authored scorecard rationales into `data/ship-ratings/`: turns each factor's editorial `mastery` (0–100) into integer `earned` points reconciled (largest-remainder) so they **sum exactly to the ship's rating**, and writes the `scorecard` block per ship. One-shot authoring aid; render with `build-ship-scorecards.py` after. `--check` previews. |
| `reconcile-ratings-html.py` | [reconcile-ratings-html.md](reconcile-ratings-html.md) | Pushes `data/ship-ratings/` into the guides: sets each ranking-table rscore + `--pct` bar to the canonical value, **drops** rows for excluded hulls (from by-role ladders, per-class + cost tables, and dossier peer tables), and **re-sorts** by-role ranking tables by rating descending. Idempotent; `--dry-run` previews; reports any tbody it empties (which then needs an editorial prose pass). |
| `build-ship-loadouts.py` | [build-ship-loadouts.md](build-ship-loadouts.md) | Builds the **3-State Loadout** (`table.l3` + intro + callout + Notes) and **Engineering Plan** (`table.data`) tables inside each ship dossier from **`data/ship-loadouts/<basename>.json`** — the **canonical source of truth**, a **SLEF** array (Ship Loadout Export Format) of three importable builds (Initial / A-Rated / Engineered) with editorial prose in `appCustomProperties.edbb`. Resolves FDev `Item`/blueprint/experimental symbols → display via `slef_resolve.py`; every cell carries **size + class** (`8E Power Plant`, `4A Multi-Cannon (Gimballed)`), engineered = blueprint+experimental (`G5 Overcharged + Corrosive Shell`). **Minimal-diff** string splice (no whole-file reformat); validates symbols resolve, A-Rated↔Engineered consistency, and Core sizes vs `data/ships/`. Idempotent; `--check` previews. Edit the SLEF, never the tables. |
| `reconcile-loadout-engineering.py` | [reconcile-loadout-engineering.md](reconcile-loadout-engineering.md) | Accuracy pass over `data/ship-loadouts/*.json` (SLEF): **(A)** enriches un-engineered-but-engineerable modules with the optimal blueprint — marginal ones (SCB, chaff, heat sink, limpets, cargo, DSS…) flagged *Optional / low-priority* in notes, core misses (shield boosters/gens, distributor, pulse lasers) fully engineered role-aware; truly un-engineerable modules keep `(No blueprint available)`. **(B)** validates + fixes `engineeringPlan` engineer attributions against `data/modifications/modules.json` (conservative — only when no listed engineer can reach the grade; handles the `Felicty Farseer` typo / `Tod "The Blaster" McQuinn` nickname). Dry-run by default; `--apply` writes. Re-run `build-ship-loadouts.py` after. |
| `audit-ship-loadouts.py` | [audit-ship-loadouts.md](audit-ship-loadouts.md) | Read-only deterministic audit of every `data/ship-loadouts/*.json` (3 SLEF builds) against `data/ships` (slots + bulkheads) and `data/modifications`: flags missing core slots (incl. **Bulkheads**), oversize/undersize modules, invalid Item/blueprint/experimental symbols, A-Rated↔Engineered slot drift, engineerable-but-unengineered modules, and experimental-available-but-none. `--json` emits machine-readable findings. Diagnostic only. |
| `slef_to_url.py` | [slef_to_url.md](slef_to_url.md) | Shared helper: SLEF build → **Coriolis** / **EDSY** "open in planner" URLs (`percent_escape(base64(gzip(json)))` of a Journal `Loadout` event; ship display name → FDev journal symbol via `data/fdev/shipyard.csv`) + a one-state SLEF string for **Copy SLEF**. Imported by `build-ship-loadouts.py` to render the §3-State Loadout export rows. No CLI. |
| `slef_resolve.py` | [slef_resolve.md](slef_resolve.md) | Shared resolver between **SLEF** and dossier display strings, driven by coriolis-data in `data/`: module `Item` symbol ↔ `{size,rating,mount,name}`, blueprint `fdname` → name, experimental edname → name, and `Slot` name → group/label/order. Imported by `build-ship-loadouts.py`; CLI (`item`/`find`/`slot`/`bp`/`special`) is the authoring aid for getting exact symbols. |
| `update-dossier-titles.py` | [update-dossier-titles.md](update-dossier-titles.md) | Sets every `guides/ships/dossiers/*.html` `<title>` to the canonical **`<Ship> · <Role> · Ship Dossier | E:D Black Box`** form. Reads the `<Ship>` name and `<Role>` label straight from each dossier's own masthead (`h1.title` span + `.role` span), so the title always matches what the page shows (`·` = `&middot;`). `<title>`-only surgery; idempotent; `--check` previews. |
| `sort-byrole-ladders.py` | [sort-byrole-ladders.md](sort-byrole-ladders.md) | Re-sorts every `guides/ships/by-role/*.html` table by rating descending — the ranking tables (those carrying `rscore`) by their score, and the cost tables by each ship's canonical `data/ship-ratings/` value. Closes the gap where `reconcile-ratings-html.py` only re-sorts a table when it *also* changed a value, so **appended** rows (e.g. new dossier hulls added to a ladder) would otherwise sit out of order. Skips the engineering table (its `td.mod` holds module names, not ships). Stable, idempotent; `--dry-run` previews. |
| `color-class-pills.py` | [color-class-pills.md](color-class-pills.md) | Colour-codes ship **pad-class** tags: rewrites `pill neutral">Large\|Medium\|Small` to `pill pad-large` (red) / `pad-medium` (amber) / `pad-small` (green), styled by `.pill.pad-*` in the design system. Scope-guarded to ship pad-class pills only — the `pill md">Medium` difficulty tags on systems/activities pages are left alone. Idempotent; `--dry-run`. |
| `bold-roster-stat-labels.py` | [bold-roster-stat-labels.md](bold-roster-stat-labels.md) | Bolds the ship name in the by-role §02 roster stat tiles — wraps `<div class="l">NAME<small>…` as `<b>NAME</b> <small>…`, so the name reads bold and apart from its descriptor (styled by `.stat .l b` / `.stat .l small`). Only tiles with a `<small>` inside `.l` match. Idempotent; `--dry-run`. |
| `drop-empty-arx-section.py` | [drop-empty-arx-section.md](drop-empty-arx-section.md) | Removes empty "ARX Pre-Built Option" sections (the "no such pre-built; buy normally" filler) from a supplied list of dossiers: deletes the `<section>` + its quick-nav item and renumbers all `sec-num`/`qn-side` sequentially. Verifies the counts reconcile and the anchor is gone. Run `generate-anchor-files.sh` + `verify-links.py` after. Idempotent; `--dry-run`. |
| `fix-arx-table-corruption.py` | [fix-arx-table-corruption.md](fix-arx-table-corruption.md) | One-off repair: rebuilds five ARX-section spec tables whose tbody was corrupted into per-character `<td>` cells, re-pairing the exploded rows into clean `<tr><td>label</td><td>value</td></tr>`. Only touches tables showing the corruption; idempotent. |
| `add-variant-builds.py` | [add-variant-builds.md](add-variant-builds.md) | Inserts/refreshes the **"Other role builds of this ship"** pill row at the end of each dossier's §Role & Overview: for `<base>-<role>.html`, links every sibling `<base>-<other>.html` as a role-coloured `.vchip` showing its headline `NN/100` rating (alphabetical by role). Reads ratings live from sibling headlines; idempotent (replaces any existing block); singletons get none. Wrapped in `.nolink`. `--check` previews. |
| `delink-blueprint-module-refs.py` | [delink-blueprint-module-refs.md](delink-blueprint-module-refs.md) | Strips ship/role-specific chrome from `guides/engineering/blueprints.html`: unwraps the auto-applied module/blueprint **type** links (group labels, per-card tags, experimental text, mis-linked material cells) and removes the "★ recommended" experimental badge, while **keeping** the engineer links. Byte-preserving, idempotent; guards the engineer-link count. |
| `delink-lexicon-apps-refs.py` | [delink-lexicon-apps-refs.md](delink-lexicon-apps-refs.md) | Trims redundant auto-applied cross-links on two reference pages: `cmdrs-lexicon.html` (Term-column links + the bold ship-name link that duplicates the trailing Dossier link) and `third-party-apps.html` (internal links inside app cards, keeping the external `cb-ext` resource link). Byte-preserving, idempotent. |
| `extract-sources.py` | [extract-sources.md](extract-sources.md) | One-time bootstrap: parses every page's bottom-of-page **Sources** block (`section.credits`) into the canonical `data/sources/<path-mirroring-guides>.json` (164 files), capturing `lead[]`/optional `tag`/per-row `label`·`what`·`url`·`display` verbatim and **stripping inline internal cross-links** (Sources = external only). Reference-only now the canon exists; `--check` previews. |
| `build-sources.py` | [build-sources.md](build-sources.md) | Regenerates each page's **Sources** block from canonical `data/sources/**.json` — the source of truth; the credits HTML is generated (never hand-edit it). Preserves each page's positional `sec-num` + section indent; idempotent. Also rewrites `data/sources/_index.md` (every unique external URL → citing pages). Filter by path fragment; `--check` previews; `--no-index` skips the catalog. |
| `audit-sources.py` | [audit-sources.md](audit-sources.md) | Read-only deterministic audit of the Sources pipeline: coverage (every credits page ↔ a data file at its mirrored path), **external-only** (every `url` external; no internal `<a href>` in any credits block), no drift (rebuild is a no-op), and schema. Exits nonzero on any failure. |
| `sources_lib.py` | [sources_lib.md](sources_lib.md) | Shared parse/render helpers for the Sources pipeline (parse a credits block → indent/sec-num/tag/leads/rows; render the canonical block; page↔data path mapping; strip internal links). Imported by `extract-sources.py`/`build-sources.py`/`audit-sources.py`; no CLI. |
| `bp_common.py` | — | Shared loaders for the blueprints pipeline: read `data/modifications/` (blueprints/modules/specials) + the `data/modifications-extra/` corrections+editorial overlays; `fix_engineer` (typo fixes), `engineer_anchor` (name → `engineer-<slug>`), `material_category` (Raw/Manufactured/Encoded from coriolis tables), `avg_rolls` formula, `slugify`. Imported by `build-blueprints.py`/`audit-blueprints.py`; no CLI. |
| `extract-blueprint-editorial.py` | [extract-blueprint-editorial.md](extract-blueprint-editorial.md) | One-time seeder: parses the current `blueprints.html` into `data/modifications-extra/editorial.json` (per-modgroup `display`/`section`/`order`; per-card `title`/`effect`/`suit`/`ctx`), mapping each card's `data-bp` to a coriolis `fdname`. Reference only now the editorial canon exists. |
| `build-blueprints.py` | [build-blueprints.md](build-blueprints.md) | Renders the **185 blueprint cards** on `guides/engineering/blueprints.html` from canonical `data/modifications/` + the `data/modifications-extra/` corrections+editorial overlays, byte-compatible with the `.bp-*` markup. Per Roll = component qty, Avg Rolls = `{1:3,2:4,3:4,4:5,5:7}` (experimentals 1), Total = product; engineers from `modules.json`, experimentals from `specials.json`. Splices only the run of cards between the `<!-- BEGIN/END generated:blueprints -->` markers in sections 02–05; idempotent; `--check` previews. Edit the data, never the cards. |
| `audit-blueprints.py` | [audit-blueprints.md](audit-blueprints.md) | Read-only deterministic gate over `blueprints.html` ⇄ `data/modifications/`: every page material/category/per-roll/engineer/experimental matches data, every `Total == Per Roll × avg-rolls`, every `engineers.html#engineer-<slug>` resolves, every `N blueprints` count matches the rendered cards, and the Sources block holds no internal-site links. Exits nonzero on any mismatch. |
| `import-materials.sh` | [import-materials.md](import-materials.md) | Vendors the canonical **materials** data (`material.csv`, `microresources.csv`) verbatim from **EDCD/FDevIDs** into `data/materials/` (read-only; mirrors `data/fdev/shipyard.csv`). Prints the Raw/Manufactured/Encoded type counts (28/64/45). Project fixes live in `data/materials-extra/`. |
| `materials_common.py` | — | Shared loaders for the materials pipeline: read `data/materials/material.csv` + the `data/materials-extra/` corrections/editorial overlays; `displayed_grid(type)` (category→grade→name, deferring `None`/Odyssey), `raw_group_labels`, `category_order`, `slugify`, `esc`. Imported by `build-materials.py`/`audit-materials.py`; no CLI. |
| `build-materials.py` | [build-materials.md](build-materials.md) | Renders the three catalog tables (Raw 7×G1–G4, Manufactured 10×G1–G5, Encoded 6×G1–G5) on `guides/engineering/materials.html` from canonical `data/materials/material.csv` + the `data/materials-extra/` overlay, byte-compatible with the `table.data` markup. Splices only between the `<!-- BEGIN/END generated:materials -->` markers (leads/tbl-desc/callouts untouched); idempotent; `--check` previews. Edit the data, never the tables. |
| `audit-materials.py` | [audit-materials.md](audit-materials.md) | Read-only deterministic gate over `materials.html` ⇄ `data/materials/`: displayed counts (Raw 28 / Manufactured 50 / Encoded 30), every rendered name present, Raw G5 column empty, 3 marker pairs, Sources external-only. Exits nonzero on any mismatch. |
| `import-engineers.sh` | [import-engineers.md](import-engineers.md) | Vendors the canonical **engineer roster** (`engineers.csv`, 38 engineers) verbatim from **EDCD/FDevIDs** into `data/engineers/` (read-only; mirrors `data/fdev/shipyard.csv`). Ship-engineer mod grades come from coriolis `modules.json`; editorial fields live in `data/engineers-extra/`. |
| `engineers_common.py` | — | Shared loaders for the engineers pipeline: `load_roster()` (38 from `engineers.csv`, name-fixed) and `ship_mods_by_engineer()` (inverts coriolis `modules.json` → per-engineer `G<n> <blueprint group>` at max grade, via `bp_common` anchors). Imported by `build-engineers.py`/`audit-engineers.py`; no CLI. |
| `build-engineers.py` | [build-engineers.md](build-engineers.md) | Renders the **38 engineer cards** on `guides/engineering/engineers.html` from `data/engineers/engineers.csv` (roster) + coriolis `modules.json` (ship-mod grades) + the `data/engineers-extra/` editorial overlay, byte-compatible with the `.rec` card markup. Splices only between the 8 `<!-- BEGIN/END generated:engineers -->` marker pairs; idempotent; `--check` previews. Edit the data/overlay, never the cards. |
| `audit-engineers.py` | [audit-engineers.md](audit-engineers.md) | Read-only gate over `engineers.html` ⇄ data: 38 cards = roster, ship-mod grades backed by coriolis (no over-claim), `engineer-<slug>`/`blueprint-group-<slug>` anchors resolve, 8 marker pairs, Sources external-only. Exits nonzero on mismatch. |
| `extract-engineers-editorial.py` | — | One-time seeder: parses the current `engineers.html` cards → `data/engineers-extra/editorial.json` (+ on-foot mod lists → `corrections.json`), preserving each editorial field's inner HTML verbatim. Reference only (the overlay is hand-maintained after seeding). |
| `rename-to-black-box.py` | [rename-to-black-box.md](rename-to-black-box.md) | One-shot project rename **E:D Black Book → E:D Black Box**: two exact string replacements (`Black Book`→`Black Box`, `blackbook`→`blackbox` covering `ed-blackbook` URLs/package name) across every tracked text file (binaries skipped, LF preserved, the tool excludes itself). `--check` previews per-file counts. Generators + SLEF `appName` are renamed too, so regeneration is a no-op; one line-wrapped header was fixed by hand. |

**Data:** `ship-names.tsv` (`slug<TAB>Display Name`) supplies the ship grid's display
names to `generate-guides-index.sh`; extend it when a new ship dossier is added.

Generated files are overwritten on each run — edit the generator, not the output.
